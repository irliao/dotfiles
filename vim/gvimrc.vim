" Gvim Configs

" Colorscheme already defined in ~/.vimrc, but redefining it
" again here fixes issue of Mvim not loading the colorscheme
" correctly at launch
colorscheme tender

" Multi-Cursors plugin
set selection=inclusive " enables Ctrl+n for gVim
