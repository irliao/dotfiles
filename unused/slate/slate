# Slate configuration file
# Be sure to customize! See https://github.com/jigish/slate#configuring-slate
config defaultToCurrentScreen true
# config menuBarIconHidden true
config resizePercentOf screenSize

### Aliases

## Key aliases
alias hyper ctrl;shift;alt;cmd
alias super ctrl;alt;cmd

## Application aliases
alias browser   'Safari'
alias editor    'Sublime Text 2'
alias terminal  'Terminal'

## Position aliases
alias full         move screenOriginX;screenOriginY                screenSizeX;screenSizeY
alias lefthalf     move screenOriginX;screenOriginY                screenSizeX/2;screenSizeY
alias righthalf    move screenOriginX+screenSizeX/2;screenOriginY  screenSizeX/2;screenSizeY
alias topleft      corner top-left                                 resize:screenSizeX/2;screenSizeY/2
alias topright     corner top-right                                resize:screenSizeX/2;screenSizeY/2
alias bottomleft   corner bottom-left                              resize:screenSizeX/2;screenSizeY/2
alias bottomright  corner bottom-right                             resize:screenSizeX/2;screenSizeY/2
alias center       move screenOriginX+screenSizeX/4;screenOriginY+screenSizeY/4 screenSizeX/2;screenSizeY/2


# General aliases
alias sox screenOriginX
alias soy screenOriginY
alias ssx screenSizeX
alias ssy screenSizeY
# Position aliases
alias fullscreen ${sox};${soy} ${ssx};${ssy}

### Bindings

## Focus Bindings
# These should be familiar to Vim users
bind 1:${super}  focus left
bind 2:${super}  focus up
bind 3:${super}  focus down
bind 4:${super}  focus right
# bind j:${hyper}	 focus behind
# bind k:${hyper}	 focus behind

## Location bindings
# These are laid out on the left hand
# bind o:${super}  throw next
bind f:${super}  ${full}
bind w:${super}  push top bar-resize:screenSizeY/2
bind x:${super}  push bottom bar-resize:screenSizeY/2
bind a:${super}  ${lefthalf}
bind d:${super}  ${righthalf}
bind q:${super}  ${topleft}
bind e:${super}  ${topright}
bind z:${super}  ${bottomleft}
bind c:${super}  ${bottomright}
bind s:${super}  ${center}
bind g:${super}  grid
bind h:${super}  hint
bind esc:${super} relaunch
bind r:${super}		 show current
bind v:${super}		 hide current

## App bindings
# These are laid out on the left hand
bind e:${hyper}  	 focus ${editor}
bind b:${hyper}      focus ${browser}
# bind `:${hyper}      toggle ${terminal}

## Location bindings
# These are laid out to be used with the right hand
# and to complement the focus bindings
# bind o:${super}  throw next
# bind i:${super}  ${full}
# bind ;:${super}  ${lefthalf}
# bind ':${super}  ${righthalf}
# bind y:${super}  ${topleft}
# bind u:${super}  ${topright}
# bind b:${super}  ${bottomleft}
# bind n:${super}  ${bottomright}
